The contents in this folder have been copied from the following source:

DHAN: https://github.com/vinthony/ghost-free-shadow-removal
- All three scripts were converted from TF1 syntax to TF2 by running:
   tf_upgrade_v2 --infile <file_name> --outfile <new_file_name>

Changes made:
- In deshadower_v2.py:
   Lines 60 - 62:
      self.sess=tf.compat.v1.Session()
      self.sess.run(tf.compat.v1.global_variables_initializer())
      ckpt=tf.train.get_checkpoint_state(self.model) 

      Change to reduce memory usage:
         config = tf.compat.v1.ConfigProto()
         config.gpu_options.allow_growth = True 
         self.sess = tf.compat.v1.Session(config=config)
         self.sess.run(tf.compat.v1.global_variables_initializer())
         ckpt=tf.train.get_checkpoint_state(self.model)
